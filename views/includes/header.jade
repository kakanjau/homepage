//
   Created by qiaoliang on 14-4-26.
  header.navbar-default.navbar-fixed-top
    div(role='banner').container.left
        div.left
            p.navbar-brand  Welcome to
              a(href='/blog')  乔卡卡的飞仙阁
        div.right
            ul(role='tablist' style='margin: 5px auto 10px;').nav.nav-tabs.navbar-default
                if categorys
                    each cg in categorys
                        - var url = '/blog/bloglist/' + cg.category + '/1';
                        - var active = (cg.category==category)?'active':'';
                        li(role='presentation', class=active)
                                a(href = url) #{cg.name}